server:
  port: 8081
  servlet:
    context-path: /carrent

spring:
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password:
    url: jdbc:mysql://localhost:3306/car?useUnicode=true&characterEncoding=utf-8&useSSL=false
  #JPA����
  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: update
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
  #�رջ��淽�����
  thymeleaf:
    cache: false
    suffix: .html
    mode: HTML5
    encoding: utf-8
    content-type: text/html
